//EXERCISE 1: The Fortune Teller

let maximum_age = '90';
let estimated_amount_per_day = '200';

console.log(`You will need ${estimated_amount_per_day} to last you until the ripe old age of ${maximum_age}.`);
alert(`You will need ${estimated_amount_per_day} to last you until the ripe old age of ${maximum_age}.`);